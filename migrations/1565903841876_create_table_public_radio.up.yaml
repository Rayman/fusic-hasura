- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."radio"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "created_at" timestamptz NOT NULL DEFAULT now(), "name" text NOT NULL, PRIMARY
      KEY ("id") );
  type: run_sql
- args:
    name: radio
    schema: public
  type: add_existing_table_or_view
